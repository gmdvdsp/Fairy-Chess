package model;

// Represents the conjoining of all the three planes that make up the board.
// It always contains exactly three planes.
public class Board {

    public Board(Plane bot, Plane mid, Plane top) {
    }

    // METHODS:

    // MODIFIES: this
    // EFFECTS: Updates the board to a new board with pieces seeded at the correct places on all three planes.
    public void createNewGameBoard() {
    }

    // MODIFIES: this
    // EFFECTS: Updates the board constructed with the three new planes.
    public void updateBoard(Plane bot, Plane mid, Plane top) {
    }
}